repos:
  - repo: local
    hooks:
      - id: backend-pytest
        name: backend-pytest
        entry: bash -c 'cd backend && python3 -m pytest -q'
        language: system
        pass_filenames: false
